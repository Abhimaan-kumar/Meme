# Meme App Frontend (Vite + React)

Run the frontend:

1. Install dependencies:

   npm install

2. Start dev server:

   npm run dev

The dev server will start (Vite default port 5173). The frontend calls the backend inside it
